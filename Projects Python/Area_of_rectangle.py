#area of rectangle
length=float(input('Enter length of rectangle: '))
height=float(input('Enter height of rectangle: '))
def rec_area():
    area=length*height
    print(f'area of rectangle using length {length} and height {height} is: {area}')
rec_area()
